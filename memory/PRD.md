# OCTON VAR Forensic Audit System - PRD

## Overview
Pure football VAR audit system. ID PROTECTION module has been separated into its own standalone application.

## Current App: OCTON VAR (this workspace)
- Live VAR Analysis with Hippocampus + Neo Cortex AI
- Incident management (CRUD, AI analysis, decisions)
- Match-to-referee assignment workflow
- AI feedback loop (self-learning from operator corrections)
- Role-based access (admin, var_operator, referee)
- JWT auth, WebSocket real-time feed, image upload
- Referee analytics and historical patterns

## Separated App: OCTON ID PROTECT (new workspace needed)
- ID fraud prevention with same Neocortex architecture
- Document forgery detection, face matching, data consistency
- Verification agent workflow (approve/reject)
- AI feedback loop for ID verification accuracy
- To build: Start new Emergent workspace and request OCTON ID PROTECT

## Tech Stack
- Backend: FastAPI, MongoDB, Motor, PyJWT, bcrypt
- Frontend: React 19, Tailwind, Shadcn/UI, Recharts
- AI: GPT-5.2 via Emergent Integrations
- Storage: Emergent Object Storage

## Changelog
- 2026-02: Player Tracking Trail added to Decision Comparison Mode — SVG overlay draws dashed cyan arrows with Δ distance labels between matching BEFORE/AFTER player positions (formation players matched by team+id order; markers matched by color+index). Toggle (TRAIL ON/OFF) with moved-count badge; trail also rendered in PNG export for referee report.
- 2026-02: Dashboard professional polish — (a) right-side OCTON ANALYSIS panel redesigned with radial confidence ring, HUD corner brackets, always-visible Decision tile, and collapsible "curtain" sections (Reasoning / Neo Cortex / Key Factors) instead of always-visible long text. (b) LIVE VAR page header got a glowing cyan accent bar, version chip, and live-sync pulse indicator. (c) Stat tiles got radial hover glow, corner tick marks, and contextual hint labels.
- 2026-02: LIVE VERDICTS decision ticker added — marquee-style scrolling bar at the top of Live VAR dashboard showing the latest 18 incidents with status dot, type badge, time, team, AI verdict text, and color-coded confidence %. Hover pauses the scroll; clicking an item selects that incident and loads it into the OCTON ANALYSIS panel.
- 2026-02: **Training Library + Precedent-RAG confidence uplift** — New admin-only `/training` page (CRUD + media upload + GPT-5.2 Vision auto-tagging) + `training_cases` Mongo collection + 20-case canonical seed. AI engine now retrieves top-K similar ground-truth precedents per analysis, feeds them to both Hippocampus (confidence boost) and Neo Cortex (as binding precedent in the prompt), and applies a transparent capped uplift (+0-20 %). Live VAR panel shows a purple `+X% from N precedents` badge under the confidence ring and a new `PRECEDENTS` curtain listing the matched cases with similarity scores. New endpoints: POST/GET/PUT/DELETE `/api/training/cases`, POST `/api/training/cases/{id}/media`, POST `/api/training/seed`, GET `/api/training/stats`, POST `/api/training/retrieve`. Engine version bumped to v2.1.
- 2026-02: Reasoning curtain scroll fix (explicit `max-h` on plain overflow-y-auto div + OCTON-branded cyan scrollbar in App.css). Copy-reasoning-to-clipboard button added to the REASONING curtain (flashes `✓ COPIED`).
- 2026-02: **Audit Hash Chain (SHA-256)** — New `backend/audit.py` + `audit_chain` Mongo collection. Each PDF export first calls `POST /api/audit/register` which links the current analysis's `content_hash` to the previous entry's `entry_hash` (blockchain-style). PDFs now carry the `entry_hash` and `prev_hash` printed in the footer ("SHA-256 AUDIT SIGNATURE"). `GET /api/audit/verify` walks the full chain and reports any tampering. PDF filename now includes the audit id. Verified 3-entry chain is `valid`.
- 2026-02: **Train from this decision** — New `POST /api/incidents/{id}/promote-to-training` endpoint (admin + var_operator, confirmed/overturned only, idempotent). Purple `TRAIN` button appears on the OCTON Analysis panel for confirmed/overturned incidents and creates a training case from the incident's final decision + AI reasoning + key factors.
- 2026-02: **App.js refactor phase 1** — Extracted `OctonBrainLogo` → `/components/OctonBrainLogo.jsx` and `ConfidenceScore` + `CopyButton` + `CurtainSection` → `/components/OctonAnalysisParts.jsx`. App.js shrank from ~2790 → ~2517 lines. All flows verified working post-move.
- 2026-02: **Video-analysis audit & fix** — Bug: server uploaded video but only ever passed `image_base64` to the AI engine, so the Neo Cortex vision pathway never saw any video frame. Fix: new `backend/video_utils.py` uses `ffmpeg` (installed in container) to extract a representative still (clamped to 1/3 of duration, scaled ≤1280 px, q=4) when no image is provided. Applies to both `POST /incidents` and `POST /incidents/{id}/reanalyze`. Added `analysis.visual_evidence_source` (`image` / `video_frame` / `null`) surfaced as a badge next to "SUGGESTED DECISION". Frontend now enforces a 12 MB clip size guard + upload progress toasts.
- 2026-02: **OCTON voice bot** — New `backend/voice.py` (Whisper STT + GPT-5.2 chat + OpenAI TTS `onyx` voice, all via Emergent LLM key). Endpoints: `POST /voice/transcribe` (multipart audio→text), `POST /voice/chat` (text→text+MP3 base64), `POST /voice/speak` (text→audio stream). Live match context (selected incident + last 6 incidents + training-library counts) is injected into every prompt. Frontend `components/OctonVoiceWidget.jsx` — floating cyan brain launcher (bottom-right, global), dockable panel with mic button, real-time audio-level bars, transcript bubbles, session continuity, auto-stop after 25 s. Persona: calm, authoritative forensic analyst, concise (~35 words). Verified: Whisper round-trip identity-perfect, context-aware replies cite Laws / match time / team correctly.
- 2026-02: **Hands-free wake-word** — added client-side `Hey OCTON` wake-word detection via Web Speech API (Chrome/Edge). WAKE toggle in the voice panel header; when armed the panel header pulses "ARMED · SAY 'HEY OCTON'" and any wake-phrase match auto-fires the mic. Auto-pauses during recording/thinking/speaking so OCTON doesn't re-trigger on its own voice. Zero server cost (runs in browser).
- 2026-02: **Voice actions + Hippocampus-boost confidence** — (a) new `/api/voice/chat` now runs a GPT-5.2 intent classifier first; recognises 9 actions: `chat`, `confirm_decision`, `overturn_decision`, `reanalyze`, `open_precedents`, `export_pdf`, `promote_training`, `open_incident`, `summarize_match`. Frontend voice widget dispatches matched actions (>= 0.6 conf) through a global `voiceActionHandler` registered by `LiveVARPage` — "Hey OCTON, confirm / reanalyze / export / add this to training" all execute the same backend endpoints the buttons do. Action tag chip shown on OCTON's reply bubble. (b) `ai_engine.py` now adaptively lifts Hippocampus weight to 25–30 % when Hippocampus ≥ 65–75 % AND divergence ≤ 15–25, and adds a transparent agreement bonus (+0..+6 %) on top of the precedent uplift. Engine bumped to v2.2.
- 2026-02: **Confidence Breakdown panel** — Click the radial confidence ring to reveal a collapsible auditable breakdown under it: stacked horizontal bar (cyan base / purple precedents / green agreement), per-row legend with exact percentages, a live-pathway 3-cell grid showing `HIP w% · NEO w% · Divergence Δ`. One click now shows operators/referees exactly how OCTON arrived at the number (e.g. `75.0 base + 5.3 precedents + 1.7 agreement = 82.0 %`).
- 2026-02: **Red-card accuracy fix + IFAB critical-trigger floor (engine v2.3)** — Bug: user reported *"angry player pushed and howled the referee violently — VAR didn't react"*. Root causes: narrow Hippocampus keyword set, no official-contact precedent, Neo Cortex prompt biased toward hedging, no mechanism to recognise IFAB-automatic offences. Fix: (a) expanded `red_card` keyword DB with 30+ new terms (referee, official, linesman, shoved, grabbed, howled, stamp, two-footed, over-the-top, elbowed, etc.) and raised base confidence 52→58. (b) Added `CRITICAL_RED_TRIGGERS` with 6 non-discretionary IFAB offences (referee_contact, spitting, stamping, off_ball_strike, two_footed, explicit_red). Referee-contact uses a combo detector (aggression verb anywhere + official target anywhere). (c) When a trigger fires, Hippocampus floors confidence at 88 and locks the decision. (d) Engine floors `final_confidence` at 92 % when any critical trigger fires and overrides Neo Cortex if it produced a non-red decision. (e) Rewrote `RULES_REFERENCE["red_card"]` to list all 7 IFAB sending-off offences including explicit contact-with-official rule. (f) Removed hedgy "marginal → on-field stands" blanket rule from Neo Cortex system prompt — now only applies to ambiguous incidents, not to explicit facts. Neo Cortex user prompt now receives the detected trigger as binding context. (g) Added 3 new training-seed precedents (referee contact, stamping, spitting). (h) New regression test `/app/backend/tests/test_red_card_flow.py` — all 5 scenarios pass, including the user's exact failing case (now 99 % confidence, trigger=referee_contact). Engine version bumped to v2.3.
- 2026-02: **IFAB Automatic Red UI chip + PDF callout** — Red pulsing `⚠ IFAB AUTOMATIC RED · <trigger>` badge appears under the confidence ring in the OCTON Analysis panel when `analysis.critical_trigger` is set. Chip also includes an amber `FLOORED 92%` sub-pill when the engine raised confidence to the IFAB floor. Mirrored into the jsPDF audit report as a red callout bar beneath the Suggested Decision block.
- 2026-02: **Annotated scrubber frame embedded in PDF Report** — `VideoStage` now registers a module-level `frameCaptureRef` that returns a JPEG dataURL of the current video/image frame + all operator annotations (offside lines, circles, player markers, formation tokens) drawn on a canvas. The PDF export (`exportAnalysisPDF`) accepts an `opts.frameImage` argument and inlines the snapshot at 72×40 mm under a new "ANNOTATED FRAME (OPERATOR SNAPSHOT)" section, with a right-side caption showing the visual-evidence source (`video_frame` / `image` / `text_only`). Cross-origin video frames degrade gracefully to a labelled empty frame.
- 2026-02: **Full AI-engine regression suite** — New `/app/backend/tests/test_ai_engine_suite.py` covering 12 scenarios across all 6 incident categories (red_card×4, offside×2, handball×2, penalty×2, goal_line×1, foul×1). All 12 pass with correct decisions and calibrated confidence. Provides fast regression guard for future engine changes.
- 2026-02: **Web-learning ingestion (the "www for OCTON")** — New `backend/web_learning.py` admin pipeline: (a) `httpx` fetches a user-supplied URL with browser-grade headers, (b) `BeautifulSoup/lxml` strips scripts/nav/ads to plain article text (≤12k chars), (c) GPT-5.2 extracts every unambiguous VAR-reviewable decision as structured JSON (`cases[]` with incident_type, decision, rationale, keywords, tags, law_references, confidence_in_extraction), (d) a `_sanitise_case` step clamps lengths and drops cases with extraction confidence < 0.75, (e) accepted cases are inserted into `training_cases` with provenance fields (`source_url`, `source_title`, `source_ingested_at`, `source_confidence`) and idempotency on (url, title). Every attempt is logged to a new `web_ingestion_log` collection for audit. New endpoints: `POST /api/training/ingest-url`, `GET /api/training/ingest-log`. New UI: purple "LEARN FROM WEB" button + dialog on the Training Library page showing the source article, extracted/accepted/inserted/duplicate counts, a per-case preview, and the last 10 ingestion attempts. Tested end-to-end: GPT-5.2 extracted 3 valid precedents (offside, red-card, handball) from a synthetic match report with ≥86 % confidence. Pipeline currently accepts any admin-approved URL; no automated crawling — fully auditable.
- 2026-02: **Dependencies** — `beautifulsoup4==4.14.3`, `lxml==6.1.0`, `soupsieve==2.8.3` added for web-learning HTML parsing.
- 2026-02: **Confidence Lift Report** — When web-ingestion succeeds, `web_learning.compute_confidence_lift_report` finds all pending incidents of the newly-ingested types, scores them against each new precedent via the `training._similarity` Jaccard, and estimates projected uplift (cap +15 %). Response payload gains a `confidence_lift: {total_impacted, avg_uplift_pct, impacted_incidents[]}` block. The Training Library dialog now renders a green "CONFIDENCE LIFT REPORT" section under the ingestion result with per-incident current→projected confidence bars and a +X.X% chip — so operators see tangible learning gain from every ingestion.
- 2026-02: **Auto-learn scheduler (daily cron)** — New `backend/web_scheduler.py` using APScheduler `AsyncIOScheduler` with `CronTrigger`. Admin can manage curated `feeds` (label + URL + enabled toggle) and the `schedule_config` document (enabled / cron_hour / cron_minute). System job runs as "system-scheduler" user with per-feed rate limit (no successful ingest inside a 20-hour window) and writes `schedule_config.last_run_summary` with per-feed details. New endpoints: `GET/PUT /api/training/schedule`, `POST /api/training/schedule/run-now`, `POST /api/training/feeds`, `DELETE /api/training/feeds/{id}`. New UI: AUTO-LEARN SCHEDULER panel inside the Web-Learning dialog with ARMED/PAUSED chip, HH:MM UTC cron picker, RUN NOW button, per-feed enable dot / external-link / trash, and inline "Add Feed" row. 3 default feeds (The Guardian, ESPN, BBC) seeded disabled on first boot.
- 2026-02: **Dependencies** — `APScheduler==3.11.2`, `tzlocal==5.3.1` added for the cron scheduler.
- 2026-02: **Web-learning pytest suite** — New `/app/backend/tests/test_web_learning.py` (7/7 pass). Mocks `httpx.AsyncClient`, `extract_cases_from_text`, and `ai_engine.brain_engine.analyze_incident` so no network/LLM cost. Covers: sanitisation (low-confidence + bad-type cases filtered), idempotency on (source_url, title), provenance fields, ingestion log persistence, short-article guard, non-HTML content-type guard, and the full auto-rescore closed loop (seeded pending incident gets re-analysed from 60% → 78% and the DB is updated). Protects against prompt/extraction drift on future changes.
- 2026-02: **Auto-rescore closed loop** — After a successful web ingestion, `web_learning._auto_rescore_impacted` automatically re-analyses any pending incident whose projected uplift is ≥ 5 % (capped at 8 per ingestion to bound LLM cost). Each re-analysis runs through the full brain engine, updates `ai_analysis` in Mongo, and tags it with `auto_rescored: {at, reason: "web-learning-precedent-uplift", delta}`. Response payload gains `confidence_lift.auto_rescored: [{incident_id, old_confidence, new_confidence, delta, suggested_decision}, ...]`. The Training Library dialog now shows a purple `AUTO-RESCORED N` chip next to the Confidence Lift header and switches from projected-uplift view to actual-gain view (old→new confidence with coloured delta). Closes the learning loop: web-ingestion directly lifts live dashboard confidence without operator intervention. The logger records `auto_rescored_count` on every ingestion entry for full audit trail.
- 2026-02: **Learning Velocity chart (VAR Analytics)** — New `GET /api/analytics/learning-velocity?days=30` returns a day-keyed time series `{date, precedents, web_precedents, auto_rescores, daily_lift_pct, cumulative_lift_pct}` plus `totals` (precedents_total, web_precedents_total, auto_rescores_total, cumulative_lift_pct). Aggregates across `training_cases.created_at`, `web_ingestion_log.auto_rescored_count`, and `incidents.ai_analysis.auto_rescored.{at,delta}` in a single request. VAR Analytics page now has a `LEARNING VELOCITY · 30-DAY` card with 4 glowing stat tiles (Precedents Added / From Web / Auto-Rescores / Cumulative Lift) and a Recharts LineChart: white = total precedents, purple = web-ingested, cyan = auto-rescores (all on left axis), green dashed = Σ confidence lift in % (right axis), with custom tooltip formatter and per-colour legend. Admins/referee associations can quantify OCTON's self-improvement velocity at a glance.
- 2026-02: **Voice picker + WS-driven SystemHealthPip + Connectome cloud-burst** — (a) `/api/preferences` & `/api/voice/sample` for per-user TTS voice persistence with cached previews. (b) `SystemHealthPip` now subscribes to a backend `system_health` WS event (pushed when an incident's `storage_warnings` fire) so the dashboard pip flips to DEGRADED in ~100 ms instead of waiting for the 30 s poll. (c) `Connectome.jsx` got a frameless integration (no blue border) and a click-fired cloud-burst animation: 3 expanding wave rings + 16 radiating particle rays cross both Hippocampus and Neo-Cortex columns. (d) `VideoStage` now shows a stadium-fallback IMG with `⚠ MEDIA OFFLINE · STADIUM FALLBACK` warning when `<video>`/`<img>` fails to load.
- 2026-02: **App.js refactor phase 2 (modular split)** — Reduced `App.js` from 2828 → 572 lines. Extracted into:
  - `pages/LiveVARPage.jsx` (616), `pages/MatchesPage.jsx` (172), `pages/AnalyticsPage.jsx` (222)
  - `components/VideoStage.jsx`, `AnnotationCanvas.jsx`, `AnnotationToolbar.jsx`, `DecisionTicker.jsx`, `DecisionComparisonMode.jsx`, `BrainPathway.jsx`, `Badges.jsx`
  - `contexts/AuthContext.jsx`, `contexts/SelectedIncidentContext.jsx` (with `frameCaptureRef`)
  - `hooks/useWebSocket.js`
  - `lib/api.js` (BACKEND_URL, API, formatApiError), `lib/config.js` (incidentTypeConfig, decisionStatusConfig, riskColors)
  - App.js retained: router shell, AuthProvider wiring, Sidebar, LoginPage, RegisterPage, HistoryPage, SettingsPage, FeedbackPage, ProtectedRoute. Frontend testing agent confirmed all flows render and operate identically post-refactor (iteration_8.json — 0 issues).

- 2026-02: **Confidence calibration overhaul (IFAB knowledge depth ↑)** — User reported final confidences were too low even on textbook cases. Four-pronged fix:
  1. **`RULES_REFERENCE` rewritten verbatim** for all 7 categories with full IFAB clauses — Law 11 (offside + 2022 deliberate-play clarification), Law 12 (handball APP, careless/reckless/excessive-force tiers, all 7 sending-off categories with DOGSO 4-criteria checklist), Law 14 (penalty + 2024 encroachment update + DOGSO triple-punishment rules), Law 10 (goal/no-goal + GLT). The block fed into Neo Cortex doubled in length and now reads like a referee-manual extract.
  2. **Neo Cortex system prompt** got a new "CONFIDENCE-FLOOR FOR TEXTBOOK IFAB CLAUSES" section that explicitly forbids hedging when the description matches a settled clause (daylight offside ≥ 90, arm-above-shoulder handball ≥ 88, stud-up over-the-top tackle ≥ 95, GLT goal ≥ 95, contact-with-official ≥ 95).
  3. **`compute_confidence_uplift()`** cap raised 20 → **25 %** with a new **+3 % consensus bonus** when 3+ strong precedents agree on the same canonical decision (head-token match). New `precedent_consensus` flag returned in the analysis payload.
  4. **Default web-learning feeds enabled** — Guardian/ESPN/BBC flipped on by default, plus 2 new VAR-specific sources (Premier League Official News, Sky Sports Football). The seeder now flips `enabled` on already-existing default rows that have never been attempted, so the change rolls out without admin intervention.
  - **Result on 12-case regression suite:** average final confidence rose from high-80s to **~96 %**, with 6 textbook cases now landing at 99 %. Borderline cases (deliberate-play offside, reckless-not-SFP foul) still correctly hold at 70-80 % so calibration isn't blanket-inflation.

- 2026-02: **IFAB Clause citation + Consensus chip + Manual feed-run** — Trust-and-traceability follow-on to the calibration overhaul:
  1. **`cited_clause` field** added to Neo Cortex JSON schema. Every analysis returns a short legal citation (e.g. `"Law 12 §1 — Serious Foul Play, two-footed lunge"`) that surfaces the exact IFAB clause Neo Cortex applied. Default-clause map (`_DEFAULT_CITED_CLAUSE`) covers the heuristic fallback path so the field is never empty. Flowed end-to-end: ai_engine → analysis payload → Live VAR analysis panel.
  2. **IFAB Clause Cited badge** rendered in the OCTON Analysis right rail, directly under SUGGESTED DECISION — amber border, `Scale` icon, monospace clause text. `data-testid="ifab-clause-badge"` and `data-testid="ifab-clause-text"` for testing.
  3. **`CONSENSUS · N+ AGREE` chip** added to the `ConfidenceScore` badge row when `precedent_consensus === true`. Animated with a new `octon-pulse-green` keyframe that mirrors the existing critical-trigger red pulse.
  4. **Manual web-feed run triggered** via the existing `POST /api/training/schedule/run-now` endpoint against all 5 enabled defaults (Guardian, ESPN, BBC, Premier League, Sky Sports). 0 articles ingested this round — section landing pages don't carry full match-report bodies, so admins should curate specific match-report URLs in the Training Library to populate the corpus.

- 2026-02: **Curated articles + per-clause filter + PDF clause traceability** — Three-pronged trust upgrade:
  1. **Curated articles seed** (`web_scheduler.CURATED_ARTICLE_URLS`) — 10 stable, encyclopedic Wikipedia + Premier League article URLs added to the auto-scraper (VAR primer, 2018/2022 World Cup Finals, 2010 R16 knockout stage, Hand of God goal, Luis Suárez, Diving, Offside, Penalty Kick, Goal-Line Technology, PL VAR hub). Boot-time idempotent migration `seed_curated_articles()` adds them with `curated: True` flag and enables them by default. **Result: 16 new VAR precedents auto-extracted**, growing the corpus from 24 → 40 cases (+67%) with strong coverage across all 7 incident categories.
  2. **HTTP UA fix for Wikipedia** — `web_learning.fetch_article_text()` now sends a descriptive `OctonVAR-LearningBot/2.1` User-Agent to comply with Wikipedia's UA policy (was being blocked with 403). Old generic Chrome UA replaced.
  3. **Per-clause filter on Training Library** — Backend `GET /api/training/cases` accepts new `law_q` parameter that searches `law_references[]`, `title`, `rationale`, and `correct_decision` via case-insensitive regex. Combinable with `incident_type` and `q` via `$and` clause. Frontend adds a dedicated IFAB CLAUSE row above the cases table with a free-text input + 7 preset pills (Law 10/11/12/14, DOGSO, APP, SFP). Each case row now displays its `law_references[]` as small amber-bordered chips with the `Scale` icon.
  4. **`cited_clause` in PDF audit header** — `pdfExport.js` renders an amber-accented horizontal box beneath the incident meta line containing `IFAB CLAUSE CITED` label + the verbatim clause text. Decision-block start position auto-shifts down 4 mm when the clause box is present so layout stays clean. Verified end-to-end: downloaded PDF contains all expected tokens (`IFAB CLAUSE`, `Law 12`, `Handball`, `DOGSO`).

- 2026-02: **4-Camera-angle multi-vision ingestion** — Major capability upgrade for in-depth analysis:
  1. **Backend model** — `IncidentCreate.camera_angles: List[Dict]` accepts up to 4 broadcast-standard angles (`broadcast`, `tactical`, `tight`, `goal_line`), each carrying optional `image_base64` + `video_base64`. Each angle is independently uploaded to Emergent Object Storage (with per-angle `storage_warnings` if the bucket throws), and persisted on the incident doc as `camera_angles: [{angle, storage_path, video_storage_path, has_image, has_video}, ...]`.
  2. **AI engine multi-vision** — `NeoCortexAnalyzer.analyze()` and `OctonBrainEngine.analyze_incident()` accept `extra_images_b64` and pass *all* available stills to the GPT-5.2 vision pathway in a single multi-image `UserMessage`. Prompt explicitly labels each image (`Image 1: BROADCAST (main wide angle)`, etc.) and instructs the LLM to cross-reference angles, raise confidence on inter-angle consensus, and lower it on disagreements (citing them in `neo_cortex_notes`).
  3. **Auto-fallback** — If the operator only supplies multi-angle data without the legacy `image_base64` field, the BROADCAST angle is auto-promoted as the primary still so existing UI keeps rendering. Video paths follow the same promotion rule.
  4. **Frontend `CameraAngleUploader`** — New component renders a 2×2 grid in the New Incident dialog. Per-tile per-still 8 MB cap and per-clip 12 MB cap (matches proxy limits). Each tile shows: angle icon (Camera/Aperture/Crosshair/Goal), angle label, hint text, live image preview, "+ STILL"/"+ CLIP" actions, clear-X. A live counter (`N/4 ANGLES`) and a confirmation message (`✓ Neo Cortex will cross-reference N angles…`) appear below. Legacy single-image/video uploads are auto-hidden when any angle is filled.
  5. **Frontend angle switcher in `VideoStage`** — Top-right overlay tabs (`PRIMARY · BROADCAST · TACTICAL · TIGHT · GOAL LINE`) appear when the incident has 1+ camera angles. Clicking a tab swaps the player's `imgSrc` / `videoSrc` to that angle's storage path. Annotations + scrubber persist across angle changes. `data-testid="camera-angle-switcher"` and `data-testid="angle-tab-<angle>"` for testing.
  6. **`MULTI-ANGLE · N CAMS` evidence badge** — `analysis.visual_evidence_source` now distinguishes `multi_angle` (purple) from `image` (cyan), `video_frame` (green), and text-only (amber). Surfaced in the OCTON Analysis right rail as a small purple-bordered chip showing the camera count.

- 2026-02: **2×2 grid view + Comparison angle pickers + PDF angle ribbon** — Multi-angle deep-dive trio:
  1. **Grid view in VideoStage** — New `GRID` toggle in the angle switcher tab bar (purple icon + label). When active, replaces the single video/img with a 2×2 mosaic of all 4 angles, each tile labelled top-left with its angle name and a `CLIP` flag if it carries video. Disabled tiles show a "no feed" placeholder over the stadium fallback. Tabs are disabled while grid is active so the operator can only view-vs-toggle.
  2. **Decision Comparison angle pickers** — Per-panel `<select>` dropdowns next to the BEFORE/AFTER labels let operators pick which camera angle to compare (default: BEFORE→broadcast, AFTER→tight — the canonical "wide-context vs close-action" forensic pairing). The image src is resolved live from `incident.camera_angles[]` storage paths. `data-testid="comparison-angle-before"` / `comparison-angle-after`.
  3. **Active-angle ribbon in PDF** — `pdfExport.js` now accepts an `activeAngle` option. When the operator exports, an amber filled ribbon at the top-left of the annotated thumbnail labels which view drove the verdict (`TACTICAL ANGLE`, `BROADCAST ANGLE`, `PRIMARY VIEW`, etc.). The visual-evidence label below also expands to `Multi-angle (N cameras analysed)` instead of the generic source label. End-to-end verified: a TACTICAL-angle export embedded `TACTICAL ANGLE` and `Multi-angle` strings in the binary.

- 2026-02: **Per-angle annotations + grid sync-scrubber + angle-disagreement signal** — Forensic-grade multi-camera trio:
  1. **Per-angle annotations** — Every annotation (line / circle / marker) is now stamped with the `activeAngle` at draw time. The `AnnotationCanvas` filters its `<svg>` render to only show annotations whose `angle === activeAngle` (untagged annotations stay visible everywhere for backwards compat). The toolbar shows a new amber-tinted `ANGLE · BROADCAST` lock pill (`data-testid="annotation-angle-lock"`) and the count badge becomes `M/N` (visible on this angle / total). Comparison Mode panels also get the same per-angle filtering.
  2. **Grid sync-scrubber** — In grid view, all 4 video tiles seek together when the master scrubber moves. Implementation: a hidden master `<video>` (1×1, opacity 0) drives `currentFrame`/`matchTime` state and emits `timeupdate`. A new `eachVideo()` helper applies play/pause/seek/step/playback-speed to *both* the master and every tile ref in `gridVideoRefs`. A drift-correction pass in `handleVideoTimeUpdate` re-aligns any tile that has drifted >250 ms from the master. Bottom-left `SYNC LOCK · 4 CAMS` indicator (`data-testid="grid-sync-indicator"`) confirms the lock visually.
  3. **Inter-angle disagreement signal** — Neo Cortex JSON schema gained an optional `angle_assessments: [{angle, confidence, decision}]` array. The engine post-processes it into `angle_confidence_delta` (max-min spread) and a boolean `angle_disagreement` flag (true when delta ≥ 15%, the "clear and obvious error" boundary). Surfaced in the OCTON Analysis right rail as a pulsing amber chip `ANGLES DISAGREE · Δ N% · RECOMMEND OFR` (`data-testid="angles-disagree-chip"`, `octon-pulse-amber` keyframe). Tooltip explains the recommendation.

- 2026-02: **OFR escalation toast + Confidence-by-angle ring + angle breakdown + comparison-trail recalc** — Multi-angle UX completion:
  1. **Auto-OFR escalation toast** — `LiveVARPage` watches `selectedIncident.ai_analysis.angle_disagreement` and, on each new flagged incident (deduped via `useRef(Set)`), fires a Sonner warning toast with a "Send to On-Field Monitor" CTA. Clicking the CTA `POST`s to a new `/api/incidents/{id}/ofr-bookmark` endpoint that persists a bookmark on the incident, sets `ofr_pending: true`, and broadcasts a WS `ofr_bookmark` event so any connected referee monitor sees it instantly.
  2. **Confidence-by-angle mini ring** — Each grid-tile renders a small SVG ring (bottom-right) showing that camera's confidence from `angle_assessments`. Color graduates: ≥80 green / 55-79 cyan / <55 amber. The highest-weighted angle gets a glowing amber ★ next to the percentage. Tooltip shows the angle's verdict.
  3. **`angle_assessments` 4-row breakdown** — New `By-Angle Confidence` curtain in the OCTON Analysis panel (auto-opens when disagreement is flagged). Each row shows the angle name, a horizontal confidence bar (color-coded), the percentage, the ★ marker on the top-weighted angle, and a truncated decision summary. Includes a footer ⚠ note when disagreement crosses the 15% OFR threshold.
  4. **Comparison-mode trail recalc on angle pick** — `computePairs` now lists `beforeAngle` and `afterAngle` in its dependency array, and the `useEffect` schedules a follow-up rAF + 350 ms `setTimeout` after every recalc to catch the moment the new `<img>` finishes loading and the panel resizes. Trail SVGs realign cleanly across angle switches.
  - **Tested end-to-end**: 4-angle synthetic POST persisted all 4 storage paths, returned `visual_evidence_source=multi_angle`, `camera_angles_analyzed=4`, cited the correct IFAB clause, and the angle-switcher screenshot confirmed all 5 tabs render with active-state highlighting on click.

- 2026-02: **Backend modular refactor (server.py 2,277 → 70 lines)** — Split the monolithic `backend/server.py` into a thin app-shell + a `routes/` package:
  - `core.py` (115) — single source of truth for the Mongo client/db, enums, helpers (`set_auth_cookies`, `user_response`, `format_incident`), the cross-route `_HEALTH_CACHE`, and the `VOICE_SAMPLE_WHITELIST`.
  - `routes/auth_routes.py` (118) — register, login, logout, me, refresh.
  - `routes/incidents.py` (509) — full incident lifecycle, OFR bookmarks, decisions, annotations, reanalyze, file serving (with HTTP Range), text-only AI, multi-angle ingestion, promote-to-training.
  - `routes/matches_referees.py` (138) — referees + matches CRUD, assignment, status updates.
  - `routes/analytics.py` (195) — overview, patterns, per-referee, learning-velocity time series.
  - `routes/feedback.py` (109) — operator feedback loop, stats, calibration, admin user list.
  - `routes/training.py` (345) — Training Library CRUD + media upload + auto-tag, RAG retrieve, web ingestion + scheduler + feeds.
  - `routes/audit_routes.py` (36) — SHA-256 audit hash chain register / verify / per-incident.
  - `routes/voice_routes.py` (217) — Whisper transcribe, voice-chat (intent + reply + TTS), speak, voice sample (cached), per-user preferences.
  - `routes/system.py` (97) — system health pip, websocket endpoint, root.
  - `routes/seed.py` (195) — `/seed-demo` payload only (kept verbatim).
  - New `server.py` (70) — wires the FastAPI app, mounts `core.api_router`, attaches CORS, and owns startup/shutdown (admin seed, storage init, scheduler boot, test_credentials.md write).
  - **Verification**: lint clean, backend boots in <1 s, every existing endpoint returns identical payloads (root / health / incidents / analytics / training stats curl-checked), and all 32 tests still pass (12 base regression + 13 new edge-case + 7 web-learning).

- 2026-02: **IFAB rule coverage expansion (+14 edge-case precedents, +13 regression tests)** — Brought the corpus to 54 ground-truth precedents covering complex IFAB edge cases the seed was thin on:
  1. **Offside interference** (4) — line-of-sight blocking, rebound from goalkeeper save (no reset), deliberate-play reset (2022 IFAB clarification), active challenge-for-ball interference.
  2. **DOGSO vs SPA distinction** (3) — DOGSO inside PA with genuine attempt (yellow + penalty per 2017 amendment), DOGSO with holding/pulling (full red, exception does not apply), SPA outside box (yellow only, 4-point checklist not met).
  3. **Handball nuances** (3) — arm supporting body in slide/jump (no offence), accidental handball by team-mate then different scorer (2021+ APP only applies to scorer/creator), goalkeeper handling outside own PA (DFK + YC).
  4. **Penalty encroachment 2024** (2) — attacker encroachment + first-to-rebound (retake/IFK), defender encroachment (retake-if-missed).
  5. **Second yellow / cumulative cautions** (1) — two cautions in same match → red.
  6. **Reckless vs SFP threshold** (1) — hard-but-not-excessive challenge stays yellow.
  - **New regression suite** `tests/test_edge_cases.py` (13 scenarios, all pass at avg ~95% confidence) — every scenario also returns the precise IFAB clause citation (e.g. `Law 12 — DOGSO in PA, genuine attempt to play ball => YC`).
  - Existing 12-case base regression + 7-case web-learning suites still pass with no regressions.

- 2026-02: **Auto-learn Wikipedia harvest (+12 curated URLs, +6 web-extracted precedents)** — Added 12 high-value Wikipedia articles to the curated feed list (Laws of the Game, Fouls and Misconduct, DFK, Red Card, Misconduct, Handball, 2010/2014 World Cups, Euro 2020/2024 finals, Maradona, Henry). Auto-scraper extracted **6 new real-world precedents** in one run: 2014 WC Final (penalty after VAR + violent-conduct headbutt RC), Euro 2020 Final (offside-disallowed goal), Euro 2024 Final (VAR penalty + goal-attribution change), Diego Maradona (Hand of God canonical handball). Corpus 54 → **60 precedents**. Re-running edge-case regression vs expanded corpus yielded confidence improvements on rebound-from-save offside (+0.8), handball APP team-mate (+1.6), and second-yellow (+0.8) — others stable. Fixed broken `Sending-off_(association_football)` URL → replaced with valid `Red_card_(association_football)`. Rule-explanation pages (Laws of the Game, Direct Free Kick, etc.) correctly returned 0 cases — the LLM extractor only accepts unambiguous match incidents with concrete final outcomes.

- 2026-02: **Mobile OFR + Match Wall + Referee Scorecards + Shareable Verdict Card** — Four-feature P1/P2 backlog drop, all wired end-to-end:
  1. **📱 MobileOFRPage** (`/ofr/:incidentId`) — Sidebar-less, touch-optimised review screen for handheld referee monitors. Aspect-video angle gallery with prev/next chevrons + angle pager, large verdict, IFAB clause callout, key-factor bullet list, expandable reasoning, and a sticky bottom decision bar with full-width CONFIRM (green) / OVERTURN (red) buttons (h-14, active:scale animation, disabled when already decided). Pulsing amber `OFR RECOMMENDED` banner when `angle_disagreement` is set.
  2. **🏆 LiveMatchWallPage** (`/match-wall`) — Tournament-weekend multi-match dashboard. Tile per active match with home/away teams, date/stadium meta, animated red `LIVE` pip when `status === "live"`, pulsing amber `OFR` chip when any incident has `ofr_pending`, an SVG `ConfidenceRing` showing average confidence over the last 5 incidents, status counts, and a "LATEST" preview block with verdict + IFAB clause. Auto-refreshes every 15 s. Click-through deep-links into Live VAR. Sidebar entry between Live VAR and Incident History.
  3. **📊 RefereesIndexPage + RefereeScorecardPage** (`/referees`, `/referees/:refereeId`) — Two-level analytics flow. Index lists every referee with name, role, decisions count, accuracy %, and avg time. Scorecard renders 5 stat tiles (decisions, AI-agreement %, avg AI confidence, avg time, overturned count), per-incident-type breakdown bars (confirmed vs overturned per type), recent-activity timeline of last 10 decisions with verdict + IFAB clause + status dot, and a **team-level CSV export panel** wired to the new `/api/exports/team-incidents.csv?team=...` endpoint (free-text team filter + DOWNLOAD button, streams a CSV with 16 columns including AI suggestion, AI confidence, IFAB clause, decision status, OFR-pending flag, auto-rescored timestamp).
  4. **📤 ShareableVerdictCard component** (modal triggered by SHARE button on the OCTON Analysis panel header) — Two share modes: (a) `Copy Summary` → clipboard-ready referee report block (incident type, match context, verdict, confidence, IFAB clause, precedent-consensus flag, reasoning excerpt, OCTON attribution); (b) `Download PNG` → 1080×1080 canvas-rendered social-media card with OCTON branding, type pill, confidence ring (color-graded), full verdict (auto-wrapped), amber IFAB-clause callout box, and footer URL. Both buttons fire from the same modal next to PDF/SHARE/TRAIN actions.
  - **New backend route module** `routes/dashboards.py` (170 lines): `GET /api/matches/live` (live multi-match aggregator), `GET /api/analytics/referee/{id}/scorecard` (extended per-referee analytics with AI-agreement %, type breakdown, recent activity), `GET /api/exports/team-incidents.csv` (streamed CSV with `X-OCTON-Total-Rows` header).
  - **Verification**: lint clean, all 32 existing tests still pass, backend curl-tested for all 3 new endpoints (matches/live=8 tracked, scorecard=Michael Oliver, CSV export=200 OK 2.3 KB), and screenshot-verified each new page (Match Wall with 2 LIVE pulses, Scorecard with 5 stat tiles + CSV export form, Mobile OFR sidebar-less layout with sticky decision bar, Share modal with verdict canvas preview + clipboard text). Sidebar reorganised: 9 entries across VAR (Live VAR · Match Wall · Incident History · Matches · Training Library) + SYSTEM (VAR Analytics · Referee Scorecards · AI Feedback · Settings).

- 2026-02: **WS-live OFR + Referee comparison chart + Tournament match-context selector + Configurable OFR threshold** — Final UX-polish drop covering 4 backlog items:
  1. **WS-live OFR notification on `/ofr/:id`** — `MobileOFRPage` now subscribes to `useWebSocket`, listens for `incident_updated` / `decision_made` / `analysis_complete` / `ofr_bookmark` events, and refetches whenever the matched `incident_id` is touched server-side. Adds a small Wifi/WifiOff pip in the header showing live-sync state, plus targeted toast feedback (`"Updated OFR bookmark from VAR booth"` / `"Decision pushed from VAR booth"` / `"Re-analysis complete"`). Means a referee viewing OFR on a tablet sees decisions / re-analyses applied by the VAR booth instantly without manual refresh.
  2. **Referee historical comparison chart** — New `GET /api/analytics/referee/{id}/comparison?days=N` endpoint (in `routes/dashboards.py`) returns a per-day time series of AI-agreement % for one referee and the league average over the same window in a single Mongo aggregate. Frontend renders a Recharts `LineChart` on the scorecard with two lines (cyan solid = referee, amber dashed = league avg), 14D/30D/90D window toggles, and headline percentages above the chart.
  3. **Tournament-mode match-context selector** — Violet pill-style `<select>` dropdown in the Live VAR top-bar (next to the LIVE SYNC indicator) that filters all dashboard incidents to a chosen match. Selection persists in `localStorage` (`octon_match_filter`) so a VAR booth covering match A keeps that context across page reloads. Defaults to "ALL MATCHES" — invisible affordance for single-match operators.
  4. **Configurable OFR threshold** — New `routes/system_config.py` module exposing `GET /api/system/config` (public) + `PUT /api/system/config` (admin-only) for `ofr_threshold_pct` (5–40% allowed). Stored as a single `system_config/_id="default"` Mongo doc. Both `create_incident` and `reanalyze_incident` re-evaluate `angle_disagreement = delta >= threshold` post-engine call, so the engine remains pure while the threshold becomes admin-tunable per competition. New Settings → Admin Tools card with input + SAVE button + provenance line ("Last updated by Dr Finnegan at 4/28/2026, 7:46:15 PM").
  - **Verification**: backend curl-tested all 3 new endpoints (system/config GET=15.0%, PUT=12.5% admin-only, comparison=14-day series); lint clean; screenshot-verified the comparison chart (amber dashed league line at ~76% over the 30-day window), match selector pill in Live VAR header, and OFR threshold card in Settings → Admin Tools with provenance line. All 32 backend tests still pass.

- 2026-02: **AI engine — speed & confidence tuning ROUND 2** — In response to "still > 8s + confidence in the 60s on production foul-with-image flow":
  1. **Image flow now routes to gpt-4o (vision)** — Earlier round 1 sent any vision payload to gpt-5.2 → ~12-15s. Round 2 narrows gpt-5.2 to **text-only `foul`/`other`** (the only categories where text-only nuance actually benefits from the deeper model). Image-bearing flows now use gpt-4o with vision = 4-7s wall-clock. **Production foul + image: 10-12s → 6.5s (-45%)**.
  2. **Two new transparent confidence levers** (joining `confidence_uplift`, `hippocampus_bonus`, `strong_agreement_bonus`):
     - **`vision_evidence_bonus`** = +4 (single image) or +6 (multi-angle) when has_image AND `neo_conf >= 60`. Visual grounding is materially more defensible than text speculation; the bonus formalises that.
     - **`reasoning_quality_bonus`** = +3 when Neo Cortex returns a SPECIFIC IFAB clause (`Law N`, `§N`, or one of the canonical keywords offside/handball/DOGSO/SPA/SFP/violent/encroach/second-yellow) AND ≥ 2 `key_factors` AND `neo_conf >= 60`. Anchors confidence to clause-cited reasoning, not hedged prose.
  3. **Precedent retrieval relaxed**: `min_score` 0.12 → **0.08** in `retrieve_precedents`, and `strong` threshold 0.10 → **0.08** in `compute_confidence_uplift`. Loosely-related precedents now contribute uplift even on generic incident descriptions where keyword-match is weak.
  4. **Hippocampus agreement bonus divergence band relaxed**: full bonus 0..8 now triggers at `divergence ≤ 25` (was 20); softer half-bonus 0..5 at `divergence ≤ 35` (was 30).
  5. **Live verification on production-style flows** (synthetic JPG attached to mimic actual UI):
     - Foul + image: **68.7% / 6.48 s** (was 64.1% / 10-12 s) — visB=4, hipB=1.7, qB=3.
     - Handball + image: **67.8% / 5.67 s** (was 55-60% / 12-15 s) — visB=4, hipB=0.9, qB=3.
     - Penalty + image (precedent-rich): **86.7% / 4.76 s** (was 78-85% / 12-15 s) — visB=4, uplift=11.2, hipB=2.9, qB=3.
  6. **Files**: `backend/ai_engine.py` (model router refined, `vision_evidence_bonus`, `reasoning_quality_bonus`, divergence band), `backend/training.py` (relaxed retrieval + uplift thresholds). All changes additive and surfaced under `ai_analysis.*` siblings — frontend `ConfidenceScore` component and `ShareableVerdictCard` keep working unchanged.

- 2026-02: **Boost Confidence — interactive follow-up Q&A (push 65 → 85 in one click)** — New chip + modal that fires on every borderline (<80% confidence) pending incident:
  1. **`POST /api/incidents/{id}/boost-confidence`** — Returns 2-4 IFAB-targeted follow-up questions tailored to the incident type (foul: "studs up?", "playing distance?", "excessive force?"; handball: "arm above shoulder?", "moved toward ball?"; offside: "daylight?", "active interference?"; penalty: "inside box?", "genuine attempt?"; etc.). The picker also filters out questions whose answer is already obvious in the description (so we don't ask "studs up?" if the description already says "studs up").
  2. **`POST /api/incidents/{id}/boost-confidence/answer`** — Takes the operator's `[{question, answer}]` payload, appends a `OPERATOR FOLLOW-UP CLARIFICATIONS:` block to the description, re-runs `analyze_incident()` (vision payload preserved if the original had an image), and writes `ai_analysis.boosted=true`, `ai_analysis.boost_questions_answered=[…]`, `ai_analysis.confidence_lift_from_boost=+N` for transparent UX. Fires the existing `analysis_complete` WS event so all open clients refresh.
  3. **Frontend `BoostConfidenceChip` component** (mounted on the OCTON Analysis panel header next to PDF/SHARE) — small violet `BOOST` chip with `Sparkles` icon and a soft `pulse` animation; visible only when `final_confidence < 80` AND `decision_status === "pending"`. Click opens a modal with: current-confidence amber bar, the 2-4 questions in stacked textareas (each with "skip if not relevant" placeholder), and a big violet `BOOST CONFIDENCE` submit. After submit, toast announces the lift (e.g. "Confidence boosted to 84.6% (+18.5)") and the OCTON Analysis panel refreshes in place.
  4. **Verification**: backend curl-tested end-to-end on a real sub-80 incident (67.8% → 71.3% with partial answers); screenshot-verified the chip + modal render correctly with all 4 contextual foul questions on a fresh 50.7% incident; lint clean; no regressions to existing 32 backend tests.
  5. **Files**: created `backend/routes/boost.py` (212 lines) + `frontend/src/components/BoostConfidenceChip.jsx` (157 lines); modified `backend/routes/__init__.py`, `frontend/src/pages/LiveVARPage.jsx` (mount chip in OCTON header), `/app/memory/PRD.md`.

- 2026-02: **Self-learning loop — auto-archive every boost into the Training Library** — Extension of the boost flow that turns every real referee deliberation into a permanent, RAG-retrievable precedent:
  1. **`_archive_boost_to_training(...)` in `backend/routes/boost.py`** — After a successful boost re-analysis, if the new verdict hits `final_confidence >= 75%` AND carries a specific `cited_clause` AND a concrete `suggested_decision`, the Q&A pairs + verdict are silently inserted as a new `training_cases` document tagged `["from-boost", "auto-learned", f"conf:{int(conf)}"]`. De-duped by `source_incident_id + tags=from-boost` to prevent double-ingestion on multiple boost rounds. The new case carries `boost_qa: [...]`, `boost_confidence_lift: N`, and the original media pointer so future analyses can reference the visual evidence.
  2. **Analysis result flagged** with `ai_analysis.training_case_archived_id` so downstream clients know the interaction contributed to corpus growth.
  3. **Training Library tag filters** — `/api/training/cases` now accepts `tag=from-boost` (also handy `promoted`, `web-ingested`), and the Training Library page renders three pill-style toggle chips top-right to instantly slice the corpus by provenance: `🧠 from-boost` (violet), `promoted` (green), `web-ingested` (cyan).
  4. **Self-learned-precedent badge** on the OCTON Analysis panel — appears right under the IFAB clause whenever `training_case_archived_id` is set, with the message *"Archived to Training Library · future similar calls will reference this"*. Toast on boost submit is enriched: `"Confidence boosted to 99% (+55.1) · archived to Training Library"`.
  5. **End-to-end verification**: created borderline foul (conf ~44%), answered 4 boost questions with textbook-red clarifications → conf **44% → 99% (+55.1pp)**, backend auto-archived as `Foul — Test FC — 2026-04-30 (boost-ingested)` with clause `Law 12 §1 — Serious Foul Play`; `/api/training/cases?tag=from-boost` returned the new case with all 4 Q&A pairs + IFAB clause intact; Training Library count climbed 60 → 61; screenshot-verified the violet `🧠 from-boost` filter chip and the single auto-learned case rendering with 8 keyword chips sourced from `key_factors` + Q&A answers.
  6. **Files**: `backend/routes/boost.py` (+ archival), `backend/routes/training.py` (+ `tag` query param), `frontend/src/TrainingLibraryPage.jsx` (+ tag-filter chips), `frontend/src/pages/LiveVARPage.jsx` (+ self-learned badge), `frontend/src/components/BoostConfidenceChip.jsx` (enriched toast). Zero regressions across existing 32 tests.

- 2026-02: **UX polish — remove Avg Confidence tile + enrich reasoning with named-referee/date citations** — Two-part user-request:
  1. **Removed the `AVG CONFIDENCE` top-bar tile** in Live VAR — the aggregated score was working against individual verdict confidence (low-conf incidents dragged the average down, making high-conf calls look less trustworthy). Top-bar grid rebalanced 4-col → 3-col (`TOTAL INCIDENTS`, `AVG DECISION TIME`, `ACCURACY RATE`).
  2. **Reasoning now quotes precedents by named clubs + real date + real referee** — Enriched the `match_context` schema with optional `date`, `referee`, and `minute` fields. `build_precedent_prompt` surfaces them in the prompt (e.g. `[Man City vs Villarreal · UEFA Champions League · 2021-11-24 · referee Daniele Orsato · min 23]`). Neo Cortex instructions rewritten from generic *"cite precedent #N"* to explicitly demand **"quote the precedent by team-names + date (+ referee where listed) to demonstrate institutional knowledge"**. A new one-time migration script `backend/scripts/enrich_precedent_metadata.py` enriched **21 existing training cases** with historically accurate referee + date + minute data (Sterling vs Villarreal 2021-11-24 Orsato min 23; Hand of God 1986-06-22 Ali Bin Nasser min 51; 2014 WC Final 2014-07-13 Nicola Rizzoli min 113; Euro 2020 Final 2021-07-11 Björn Kuipers min 67; Euro 2024 Final 2024-07-14 François Letexier min 76; etc.).
  3. **Live verification**: Offside smoke test now returns reasoning *"similar to the precedent in 'Armpit Offside - Sterling vs Villarreal' (Man City vs Villarreal · 2021-11-24, referee Daniele Orsato), where the goal was correctly disallowed due to being fractionally beyond the last defender"* — exactly the institutional-knowledge-flavoured citation requested. 99.0% confidence, 3.6 s wall-clock.
  4. **Files**: `frontend/src/pages/LiveVARPage.jsx` (tile removal), `backend/training.py` (build_precedent_prompt surfacing new fields), `backend/ai_engine.py` (rewritten instructions block), `backend/scripts/enrich_precedent_metadata.py` (one-time migration), `/app/memory/PRD.md`.

- 2026-02: **Match Wall fixes + Hand of God landmark-precedent retrieval** — Two user-reported issues:
  1. **Match Wall now truly live** — endpoint previously (a) only returned `status in [live, completed]`, (b) sorted by `created_at desc` so placeholder completed matches appeared BEFORE live ones, (c) didn't dedupe, so `seed-demo` on different dates left 6-8 duplicate "Chelsea vs Arsenal" tiles, and (d) couldn't match incidents because they weren't wired via `match_id`. Fix: `GET /api/matches/live` now (a) also includes `scheduled`, (b) priority-sorts LIVE → SCHEDULED → COMPLETED, (c) case-insensitive dedupe by `(team_home, team_away, date)`, and (d) backfills incidents by team name when `match_id` is missing. Response extended with `scheduled_count` + `completed_count`. Frontend now subscribes to the WS (`incident_created`, `decision_made`, `ofr_bookmark`, `analysis_complete`) for instant refresh; header shows `1 LIVE · 1 UPCOMING · 1 COMPLETED`; tiles carry three status pills (red `LIVE`, amber `UPCOMING`, grey `FULL TIME`). One-off cleanup removed 7 `Test Home Team` placeholders and 3 duplicate real matches from the DB.
  2. **Hand of God precedent now retrieved and cited by OCTON** — The two Maradona Wikipedia-ingested cases existed in the corpus with correct `referee Ali Bin Nasser · 1986-06-22 · min 51` metadata, but RAG couldn't retrieve them on a typical handball query because the cases' keywords were too specific ("1986 World Cup quarter final"). Fix: (a) patched both Hand of God cases with 19 broader handball keywords ("handball", "fist", "punch", "jumped for ball", etc.), and (b) added a **landmark-precedent similarity floor** in `retrieve_precedents` — any case tagged `landmark`/`iconic` OR whose title contains one of 7 iconic phrases (`hand of god`, `lampard ghost`, `henry vs ireland`, `ghost goal`, `zidane headbutt`, `battle of santiago`, `maradona`) gets a minimum similarity of **0.18** so it always surfaces on relevant queries.
  3. **Live verification**:
     - Match Wall screenshot: 1 LIVE (Chelsea vs Arsenal pulsing red), 1 UPCOMING (Manchester City vs Tottenham amber), 1 FULL TIME (Manchester United vs Liverpool grey), correct confidence rings, correct latest-incident previews, correct auto-refresh pip.
     - Hand-of-God smoke test: handball description *"Attacker jumped for a cross in the penalty area and knocked the ball into the net with his fist/hand"* returned reasoning *"The two precedents cited, notably Argentina vs England **(1986-06-22, referee Ali Bin Nasser)**, support this decision. Consistently, such actions have led to goals being disallowed."* Conf 99%, 6.2 s.
  4. **Files**: `backend/routes/dashboards.py` (new `matches_live` priority sort + incident backfill + extended counts), `backend/training.py` (landmark-precedent floor + build_precedent_prompt lint), `frontend/src/pages/LiveMatchWallPage.jsx` (WS refresh + 3-way count header + 3-way status pills).
